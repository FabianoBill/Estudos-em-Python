def maximo(a, b):
    """ Função que retorna o valor máximo entre dois parâmetros.
    >>> maximo(3, 4)
    4
    >>> maximo(0, -1)
    0

    :param a: number
    :param b: number
    :return: number

    """
    if a > b:
        return a
    else:
        return b

